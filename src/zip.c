#include "zip.h"


void readZip(FILE* fp) {
	//fread(void prt, size_t size of elements, size_t number of elements, fp);
    //fread(void prt, size_t size of elements, size_t number of elements, fp);
}
